let nota1, nota2, media, faltas
nota1 = 6
nota2 = 8
faltas = 6
 media = (nota1 + nota2)/2
 if (media >= 7 && faltas <= 5) {
    console.log(`O jovem está reprovado com a media ${media} e quantidade de ${faltas} faltas`);
 } else { console.log(`O jovem está aprovado com a media ${media} e quantidade de ${faltas} faltas`);
    
 } 